# Base URL of the Rust (axum) API. The web app proxies all data reads through this.
# Public — exposed to the client bundle. Leave unset to run on fixture data only.
PUBLIC_API_BASE=http://localhost:8080
